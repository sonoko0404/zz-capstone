# Optional API endpoint for future Jira or backend integration.
# Leave empty for the frontend-only MVP mock submit flow.
VITE_INTAKE_API_URL=

# Optional API endpoint for future email-only authentication.
# Leave empty for the local mock email session.
VITE_AUTH_API_URL=
